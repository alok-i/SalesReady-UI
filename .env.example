# Server-only URL used by the Next.js backend-for-frontend route handlers.
# Run salesready-api with PORT=3001 while Next.js uses port 3000.
API_URL=http://localhost:3005/v1
# Public base URL for the SalesReady backend API.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
